<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Стиль для анимации перехода между активностями -->
    <style name="Theme.DriftNotes.SlideTransition">
        <item name="android:windowEnterAnimation">@anim/slide_in_right</item>
        <item name="android:windowExitAnimation">@anim/slide_out_left</item>
    </style>

    <!-- Стиль для анимации возврата -->
    <style name="Theme.DriftNotes.SlideTransitionBack">
        <item name="android:windowEnterAnimation">@anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@anim/slide_out_right</item>
    </style>

    <!-- Стиль для плавного появления и исчезновения -->
    <style name="Theme.DriftNotes.FadeTransition">
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
    </style>
</resources>